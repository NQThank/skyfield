import React, { memo } from 'react';

interface JobDetailInfoProps {}

const JobDetailInfo: React.FC<JobDetailInfoProps> = () => {
  return <div>JobDetailInfo</div>;
};

export default memo(JobDetailInfo);
